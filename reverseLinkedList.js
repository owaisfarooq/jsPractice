class Node {
    constructor(value, tail) {
        this.value = value;
        this.tail = tail;
        // next is either set to the next Node in the list, or to None (or null) to indicate the end of the list
      }    
}


function reverseList(head) {
    
};